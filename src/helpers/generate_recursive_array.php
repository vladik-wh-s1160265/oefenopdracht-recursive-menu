<?php

/**
 * Generates a hierarchical array based on parent key of a normal array.
 *
 * @uses \count, generate_recursive_array
 *
 * @param array $menu
 * @param array $options
 * @param int|null $parent
 * @return array|null
 */
function generate_recursive_array(array $menu, array $options = [], int $parent = null)
{
    return null;
}
